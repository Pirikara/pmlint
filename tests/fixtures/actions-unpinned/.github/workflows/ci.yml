name: ci
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0 # v4 (pinned)
      - uses: actions/setup-node@v4
      - uses: some/third-party-action@main
      - uses: ./.github/actions/local-thing
